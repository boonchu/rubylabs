# learn gem
gem install httparty

code requires pretty print (pp) and httparty

ruby Coursera.rb > python.json

# learn bundler
require Gemfile to resolve dependencies
source 'http://rubygems.org'
gem 'httparty'

bundler install
bundler update

# learn rails

cd my_first_app
bundler install
bundler update
rails s

access http://localhost:3000/courses/index
search term http://localhost:3000/courses/index?looking_for=python

# learn heroku
install heroku client app from toolbelt.heroku.com
change Gemfile to segegate environment between development and production
brew install postgresql
bundle
heroku login
heroku create [instance-name]
$ git remote -v
heroku  https://git.heroku.com/search-coursera-bigchoo.git (fetch)
heroku  https://git.heroku.com/search-coursera-bigchoo.git (push)
$ git push heroku master

# learn rspec

gem install rspec
gem install selenium-webdriver
gem install capybara
gem install poltergeist
rspec -f d

```shell

Coursera App
  displays 'The Meat We Eat' when looking_for=diet
  visit root
    displays 'Johns Hopkins' (default)
    displays table element that has a row with 3 columns
    column 1 should have the thumbnail inside img tag

Finished in 3.04 seconds (files took 0.3181 seconds to load)
4 examples, 0 failures
```

# learn phantom.js
brew install node
npm install -g phantomjs

# learn debuging rails
gem byebug: debugging and executing
em webconsole: ability to have your IRB console in your browser
